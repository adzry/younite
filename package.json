{
  "name": "younite",
  "version": "1.0.0",
  "main": "src/api/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/api/index.js",
    "dev": "nodemon src/api/index.js",
    "build": "echo 'Expo/EAS handles app build'",
    "build:android": "eas-cli build --platform android --profile preview --non-interactive",
    "deploy": "railway up --service younite-api",
    "logs": "railway logs --service younite-api",
    "postbuild": "mkdir -p artifacts && echo \"📦 Saving build + logs...\" && cp -f railway.log artifacts/railway-$(date +%Y%m%d-%H%M%S).log && tar -czf artifacts/younite-logs-$(date +%Y%m%d-%H%M%S).tar.gz railway.log || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "expo-dev-client": "^5.2.4",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20.19.4"
  },
  "devDependencies": {
    "eas-cli": "3.19.2",
    "railway": "3.16.2"
  }
}
